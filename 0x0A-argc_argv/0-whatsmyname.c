/**
 * main - is good fucvcation and so helthy
 * @argc:ine prameter count
 * @argv:is ggod prameter so good
 * Return:return the prameter value
 */


#include <stdio.h>

int main(int argc, char *argv[])
{
	int i;

	for (i = 0; i < argc; i++)

	{
		printf("%s\n", argv[i]);
	}

	return (0);
}
