/**
 * main - the fucation is good so good
 * @argc:is ggod prameter
 * @argv:is more good and good
 * Return:return always 0
 */

#include <stdio.h>
int main(int argc, char *argv[])
{
	int i;

	for (i = 0; i < argc; i++)
	{
		printf("%s\n", argv[i]);
	}
	return (0);
}
